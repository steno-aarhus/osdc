# Parse the logic strings into R expressions

Parse the logic strings into R expressions

## Usage

``` r
logic_as_expression(logic)
```

## Arguments

- logic:

  The name of the logic to use.

## Value

An R expression.
