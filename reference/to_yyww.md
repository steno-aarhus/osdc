# Transform date(s) to the format `yyww`

Transform date(s) to the format `yyww`

## Usage

``` r
to_yyww(x)
```

## Arguments

- x:

  A date or a vector of dates.

## Value

A vector of dates in the format `yyww`.
